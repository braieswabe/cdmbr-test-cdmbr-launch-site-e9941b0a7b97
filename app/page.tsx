export default function Page() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", padding: "2rem" }}>
      <section style={{ maxWidth: 720 }}>
        <h1 style={{ fontSize: "2rem", marginBottom: "0.5rem" }}>Dudex Template Ready</h1>
        <p>
          This repository is configured as the base template for deterministic OpenClaw project generation.
        </p>
      </section>
    </main>
  );
}
